import React from 'react';
import Header from '../../Components/Header/Header';

const Dashboard = () => {
    return (
        <div>
            <Header></Header>
            <h1>Dashboard</h1>
        </div>
    );
};

export default Dashboard;