import React from 'react';
import Header from '../../../Components/Header/Header';

const Login = () => {
    return (
        <div>
            <Header></Header>
            <h1>Login</h1>
        </div>
    );
};

export default Login;